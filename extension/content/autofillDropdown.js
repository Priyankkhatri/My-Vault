/**
 * Autofill Dropdown
 * UI for selecting credentials within a form field.
 */

export const showDropdown = (target) => {
  console.log('Showing autofill dropdown for:', target);
  // Logic to inject dropdown UI
};
