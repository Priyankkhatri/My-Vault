/**
 * Save Prompt
 * UI for prompting the user to save newly entered credentials.
 */

export const showSavePrompt = (credentials) => {
  console.log('Asking to save credentials:', credentials);
  // Inject save UI
};
